# Somewhat more VI like copy paste.
# Use ctrl-b [ to enter copy mode, "v" to start and "y" to yank and ctrl-b ] to paste
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection
